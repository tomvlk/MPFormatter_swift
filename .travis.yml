language: objective-c
osx_image: beta-xcode6.3

before_install: 
- brew update
- if brew outdated | grep -qx xctool; then brew upgrade xctool; fi

script: 
- xctool clean build -project MPFormatter.xcodeproj -scheme MPFormatter CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO
